# Production requirements with pinned versions
# Last updated: 2026-09-14
# Python: >=3.10 (required for Django 5.1)

# Web Server
gunicorn==23.0.0
whitenoise==6.12.0

# Django Core Framework
Django==5.1.15
djangorestframework==3.17.2
django-environ==0.11.2

# Django Extensions & Apps
django-import-export==4.3.3
django-json-widget==2.0.1
django-markdownify==0.9.5
django-allauth[mfa,saml,socialaccount,steam]==65.14.1
django-ses==4.2.0
django-bootstrap5==24.3
fido2==1.2.0

# Django Extensions Requirements
jsonref==1.1.0
jsonschema==4.23.0
jsonschema-specifications==2024.10.1
Markdown==3.5.2
PyYAML==6.0.2
martor==1.6.44
setuptools==83.0.0

# Database
psycopg2-binary==2.9.10

# Email
aiosmtpd==1.4.6
dkimpy==1.1.8

# Cloud Services
azure-monitor-opentelemetry==1.6.4
boto3==1.35.75
pygithub==2.5.0

# AI providers
#
# The application talks to providers through `parodynews.ai`, never directly.
# `claude-agent-sdk` is the default provider (Claude Code, authenticated with a
# CLAUDE_CODE_OAUTH_TOKEN) and bundles the Claude Code CLI binary it drives.
# The other two are optional at runtime: a provider whose SDK is missing raises
# a configuration error only when something actually selects it.
claude-agent-sdk==0.2.152
anthropic==1.5.0
openai==3.13.0
