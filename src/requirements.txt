# Production requirements with pinned versions
# Last updated: 2026-09-14
# Python: >=3.10 (required for Django 5.2)

# Web Server
gunicorn==23.0.0
whitenoise==6.12.0

# Django Core Framework
# 5.2.x is the current LTS. 5.1.15 carried six advisories the scoped
# dependency scan reports (#176): CVE-2026-48587, CVE-2026-6873 and
# CVE-2026-8404 (fixed in 5.2.15) and CVE-2026-53877, CVE-2026-53878 and
# CVE-2026-48588 (fixed in 5.2.16).
Django==5.2.17
djangorestframework==3.17.2
django-environ==0.11.2

# Django Extensions & Apps
django-import-export==4.3.3
django-json-widget==2.0.1
django-markdownify==0.9.5
django-allauth[mfa,saml,socialaccount,steam]==65.14.1
django-ses==4.2.0
django-bootstrap5==24.3
# fido2 1.2.0 caps cryptography at <45, which pins the tree to 44.0.3 and its
# six open advisories (PYSEC-2026-2141, PYSEC-2026-35, PYSEC-2026-3552/3553/
# 3554, GHSA-537c-gmf6-5ccf). 2.x lifts the cap. Reached only through
# django-allauth[mfa]'s WebAuthn support.
fido2==2.2.1

# Django Extensions Requirements
jsonref==1.1.0
jsonschema==4.23.0
jsonschema-specifications==2024.10.1
# Markdown 3.5.2 is vulnerable to CVE-2025-69534 (uncaught exception on
# malformed HTML), fixed in 3.8.1. martor <1.7 caps Markdown at <3.6, so the
# patch is only reachable by upgrading martor alongside it.
Markdown==3.8.2
PyYAML==6.0.2
martor==1.8.2
setuptools==83.0.0

# Database
psycopg2-binary==2.9.10

# Email
aiosmtpd==1.4.6
dkimpy==1.1.8

# Cloud Services
azure-monitor-opentelemetry==1.6.4
boto3==1.35.75
pygithub==2.5.0

# AI providers
#
# The application talks to providers through `parodynews.ai`, never directly.
# `claude-agent-sdk` is the default provider (Claude Code, authenticated with a
# CLAUDE_CODE_OAUTH_TOKEN) and bundles the Claude Code CLI binary it drives.
# The other two are optional at runtime: a provider whose SDK is missing raises
# a configuration error only when something actually selects it.
claude-agent-sdk==0.2.152
anthropic==1.5.0
openai==3.13.0
