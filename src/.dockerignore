# .dockerignore
.git*
.venv*
**/*.pyc
__pycache__
# .env*
# Frontend: the Docker build runs `npm ci` itself, so a host node_modules or a
# stale dist must not be copied in.
frontend/node_modules
frontend/dist
