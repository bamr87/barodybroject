# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: barody-broject-django-postgres-flexible-aca

infra:
    provider: "bicep"
services:
    python:
        project: src
        language: py
        module: web
        host: containerapp